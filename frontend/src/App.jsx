import { Typography, Box } from "@mui/material";
import Header from "./components/Header";

const App = () => {
  return (
    <Box sx={{ padding: "2rem" }}>
      <Header/>
    </Box>
  );
};

export default App;
