import { Typography, Box } from "@mui/material";

const App = () => {
  return (
    <Box sx={{ padding: "2rem" }}>
      <Typography variant="h1">Allergen tracker</Typography>
    </Box>
  );
};

export default App;
